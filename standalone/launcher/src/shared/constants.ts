// Small circular Home button overlaid on a running app (bottom-centre). Its WebContentsView is
// sized to exactly the circle so the rest of the app stays tappable.
export const HOME_BUTTON_SIZE = 56
export const HOME_BUTTON_MARGIN = 24
